exp_name: dummy/a2c-adam
num_episodes: 1.0e+6
log_every: 100
eval_every: 100
save_every: 10000
gamma: 0.99

model:
  layer_sizes_shared: [36, 256, 256]
  layer_sizes_actor: [256, 128, 64]
  layer_sizes_critic: [256, 128, 64]

optimizer:
  name: Adam
  kwargs:
    lr: 1.0e-4
    # nesterov: True
    # momentum: 0.90
